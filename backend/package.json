{
  "name": "medcore",
  "version": "1.0.1",
  "private": true,
  "description": "medcore backend (production-first, strict tenant isolation)",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",

    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "dependencies": {
    
    "@prisma/client": "^5.18.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "pino": "^9.3.2",
    "pino-http": "^10.3.0",
    "zod": "^3.23.8",
    "bcryptjs": "^2.4.3",
"jsonwebtoken": "^9.0.2"

    
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.18.0"
    
    
  }
}
